# ARI / Asterisk
aiohttp>=3.9.0
websockets>=12.0

# FastAPI web server (dashboard API + ARI bridge)
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# STT - faster-whisper (local, CTranslate2 backend)
faster-whisper>=1.0.0

# TTS - Kokoro (82M parameter neural TTS, Apache-licensed)
# Kokoro model weights download automatically from HuggingFace on first use.
# espeak-ng (system package) is required for OOD phoneme fallback and for
# languages without Kokoro support (DE, RO, HE).
kokoro>=0.9.2
misaki[en]>=0.9.2    # phonemizer for EN, ES, FR, IT — required by Kokoro

# LLM - Ollama Python client
ollama>=0.2.0
langdetect>=1.0.9
sentencepiece>=0.2.0
transformers>=4.41.0
huggingface-hub>=0.23.0
ctranslate2>=4.4.0

# Google Calendar
google-auth>=2.29.0
google-auth-oauthlib>=1.2.0
google-api-python-client>=2.127.0

# Audio processing
soundfile>=0.12.1
numpy>=1.26.0
scipy>=1.13.0

# Voice Activity Detection — Silero VAD (replaces RMS energy-based VAD)
# Uses PyTorch for inference; the model is ~2 MB and runs on CPU in < 1 ms
# per 32 ms audio chunk.  torch + torchaudio are also needed by faster-whisper
# so this adds no new heavy dependency.
torch>=2.0.0
torchaudio>=2.0.0

# Database (call logs)
sqlalchemy>=2.0.0
aiosqlite>=0.20.0

# Config
python-dotenv>=1.0.0
pydantic>=2.7.0
pydantic-settings>=2.2.0

# Utilities
structlog>=24.1.0
