;
; Music on Hold
; Helix ships a default file-based class that plays bundled audio from
; /var/lib/asterisk/moh. Additional classes can be layered on later.
;

[general]

[default]
mode=files
directory=moh
sort=alpha
answeredonly=yes
